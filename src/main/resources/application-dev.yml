logging:
  level:
    root: info
    org:
      hibernate.SQL: error
      springframework:
        security=DEBUG:

  file:
    path: logs/PhoeStorage.log

spring:
  datasource:
    password: "%Wh@BJrX8z3uHugD7JPG#%"
    url: jdbc:mysql://188.228.52.113/phoestorage
    username: storage

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    properties:
      hibernate:
        format_sql: true
server:
  root: "C:/PhoeStorage/"